// @tilemo/data — Store + StorageAdapter + tgm: schema, backward-compatible with
// the original single-file app's localStorage. P0 stub; full port in P1.

export const TILEMO_DATA_VERSION = "0.0.0";
